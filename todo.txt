(B) Fix tables.Unshift in src/tables.lua line 46-48 to accept a val parameter instead of hardcoding 0 — change function signature from Unshift(tbl) to Unshift(tbl, val) and replace the hardcoded 0 with val +bugfix +tables
(B) Fix tables.Pop in src/tables.lua line 16 to use local keyword for the removed variable — currently leaks a global +bugfix +tables
(B) Remove src/priorities.lua and test/testPriorities.lua entirely, remove testPriorities target from Makefile line 13 and lines 36-38 and .PHONY line 51 +refactor +priorities
(B) Rewrite src/Love2DInput.lua to use native state tracking — remove calls to non-existent love.keyboard.wasPressed, love.keyboard.wasReleased, love.mouse.wasPressed, love.mouse.wasReleased; implement via internal previousKeys/currentKeys tables updated in love.keypressed/love.keyreleased/love.mousepressed/love.mousereleased callbacks, and fix GetTypedText to use a callback-accumulated buffer +bugfix +Love2D
(B) Replace math.Fibonacci in src/math.lua lines 11-13 with an iterative or memoized implementation to eliminate O(2^n) recursion +bugfix +math
(B) Refactor graphs.dijkstra in src/graphs.lua to use a min-heap from src/heaps.lua instead of O(V^2) linear scan for minimum distance vertex +refactor +graphs
(B) Fix queue memory leak in src/queues.lua — after pop, indices grow unbounded and old slots are nil but never reclaimed; compact the queue when first index exceeds a threshold (e.g. reset indices when queue is empty) +bugfix +queues
(B) Add cycle detection to graphs.topologicalSort in src/graphs.lua — currently returns a result even for cyclic directed graphs; should return nil and an error message when a cycle is detected +bugfix +graphs
(B) Add input validation to src/cache.lua LRUCache and TTLCache constructors — assert capacity > 0 and ttl > 0, error on negative or zero values +bugfix +cache
(B) Add division-by-zero guards to math.ScaleBy and math.Smooth in src/math.lua — return nil or error when denominator (fromMax - fromMin) or (upp - low) is zero +bugfix +math
(B) Fix serialize.fromJSON in src/serialize.lua to correctly parse scientific notation numbers like 1e10, 2.5e-3, -1E+4 +bugfix +serialize
(B) Fix stringify.Strip in src/stringify.lua to handle whitespace-only input consistently with Lstrip and Rstrip behavior +bugfix +stringify
(B) Update Makefile to add missing test targets: testCache, testEvents, testGraphs, testHeaps, testTries, testValidate, testSerialize, testPerformance — add individual targets and include them in the test dependency list and .PHONY +infra +makefile
(B) Add init.lua entry point at project root that requires and re-exports all src modules as a single table, so users can do local thoth = require("thoth") +feature +dx
(B) Add peek method to src/queues.lua that returns the front element without removing it +feature +queues
(B) Add delete operation to src/trees.lua supporting BST node deletion with all three cases (leaf, one child, two children via in-order successor) +feature +trees
(B) Add size and length methods to src/links.lua that traverse the linked list and return node count +feature +links
(B) Standardize src/math2D.lua to consistently use array indices {[1],[2]} for all vector operations and document this convention in a module-level comment +refactor +math2D
(B) Add pcall error boundary to EventEmitter.emit in src/events.lua lines 92-94 so one listener throwing an error does not prevent subsequent listeners from firing — collect errors and optionally expose them via an onError callback +feature +events
(B) Add SAT-based polygon collision detection to src/Love2DCollision.lua supporting convex polygons defined as vertex arrays +feature +Love2D
