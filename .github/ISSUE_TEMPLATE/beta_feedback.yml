name: Beta feedback
description: Report Phase 5 beta feedback by category.
title: "[Beta feedback]: "
body:
  - type: markdown
    attributes:
      value: |
        Use this for content beta feedback across all zones and endings. Pick one primary category so triage can sort feedback quickly.
  - type: dropdown
    id: category
    attributes:
      label: Category
      options:
        - Bug
        - Balance
        - Feel
        - Scope
    validations:
      required: true
  - type: input
    id: build
    attributes:
      label: Build or download source
      placeholder: "itch beta, GitHub release tag, or commit"
    validations:
      required: true
  - type: dropdown
    id: zone
    attributes:
      label: Zone or campaign area
      options:
        - Buried Archive
        - Salt Cistern
        - Ember Warrens
        - Estate
        - Campaign ending
        - Cross-zone
        - Unknown
    validations:
      required: true
  - type: dropdown
    id: platform
    attributes:
      label: Platform
      options:
        - Windows
        - macOS
        - Linux
        - LOVE2D from source
        - Unknown
    validations:
      required: true
  - type: textarea
    id: summary
    attributes:
      label: Summary
      description: What happened, or what felt wrong?
    validations:
      required: true
  - type: textarea
    id: reproduction
    attributes:
      label: Reproduction or play context
      description: For bugs, list steps. For balance/feel/scope, describe party, mission, week, route, and ending state.
  - type: textarea
    id: evidence
    attributes:
      label: Evidence
      description: Attach screenshots, logs, save files, replay files, or video links if available.
  - type: checkboxes
    id: permissions
    attributes:
      label: Permission
      options:
        - label: The maintainers may quote or summarize this feedback in public triage notes.
          required: false
